"use client";
import {FormEvent,useState} from "react";import "../login/login.css";
export default function ForgotPassword(){const[email,setEmail]=useState(""),[message,setMessage]=useState(""),[error,setError]=useState(""),[loading,setLoading]=useState(false);async function submit(e:FormEvent){e.preventDefault();setLoading(true);setError("");setMessage("");try{const r=await fetch("/api/auth/forgot-password",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({email})}),j=await r.json();if(!r.ok)throw new Error(j.error||"Không thể xử lý yêu cầu");setMessage(j.message||"Nếu email tồn tại, hướng dẫn đặt lại mật khẩu sẽ được gửi.")}catch(e){setError(e instanceof Error?e.message:"Không thể xử lý yêu cầu")}finally{setLoading(false)}}return <main className="login-page"><section className="login-card"><div className="login-brand"><img src="/brand/vinatex-da-nang.png" alt="Vinatex Đà Nẵng"/></div><span className="eyebrow">Bảo mật tài khoản</span><h1>Quên mật khẩu</h1><p className="login-subtitle">Nhập email tài khoản. Liên kết đặt lại mật khẩu có hiệu lực trong 30 phút.</p><form className="form" onSubmit={submit}><label>Email<input autoFocus required type="email" autoComplete="email" value={email} onChange={e=>setEmail(e.target.value)} placeholder="ten@vinatexdn.com.vn"/></label><button type="submit" disabled={loading}>{loading?"Đang xử lý...":"Gửi liên kết đặt lại"}</button>{message&&<div className="notice">✓ {message}</div>}{error&&<div className="notice">{error}</div>}</form><p style={{margin:"14px 0 0"}}><a href="/login" style={{fontSize:13,fontWeight:700,color:"#0569b5",textDecoration:"none"}}>← Quay lại đăng nhập</a></p><small className="login-footer">CỔNG THÔNG TIN HỖ TRỢ CNTT VINATEX ĐÀ NẴNG</small></section></main>}
