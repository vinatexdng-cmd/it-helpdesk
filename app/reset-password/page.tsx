"use client";
import {FormEvent,Suspense,useState} from "react";import {useSearchParams} from "next/navigation";import "../login/login.css";
function ResetPasswordForm(){const params=useSearchParams(),token=params.get("token")||"",[newPassword,setPassword]=useState(""),[confirmPassword,setConfirm]=useState(""),[message,setMessage]=useState(""),[error,setError]=useState(""),[loading,setLoading]=useState(false),[done,setDone]=useState(false);async function submit(e:FormEvent){e.preventDefault();setLoading(true);setError("");try{const r=await fetch("/api/auth/reset-password",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({token,newPassword,confirmPassword})}),j=await r.json();if(!r.ok)throw new Error(j.error||"Không thể đặt lại mật khẩu");setDone(true);setMessage(j.message)}catch(e){setError(e instanceof Error?e.message:"Không thể đặt lại mật khẩu")}finally{setLoading(false)}}return <main className="login-page"><section className="login-card"><div className="login-brand"><img src="/brand/vinatex-da-nang.png" alt="Vinatex Đà Nẵng"/></div><span className="eyebrow">Bảo mật tài khoản</span><h1>Đặt lại mật khẩu</h1>{!token?<div className="notice">Liên kết đặt lại mật khẩu không hợp lệ.</div>:done?<><div className="notice">✓ {message}</div><p><a href="/login" style={{fontWeight:700,color:"#0569b5",textDecoration:"none"}}>Đăng nhập bằng mật khẩu mới →</a></p></>:<><p className="login-subtitle">Tạo mật khẩu mới có ít nhất 8 ký tự.</p><form className="form" onSubmit={submit}><label>Mật khẩu mới<input required minLength={8} type="password" autoComplete="new-password" value={newPassword} onChange={e=>setPassword(e.target.value)}/></label><label>Xác nhận mật khẩu<input required minLength={8} type="password" autoComplete="new-password" value={confirmPassword} onChange={e=>setConfirm(e.target.value)}/></label><button type="submit" disabled={loading}>{loading?"Đang cập nhật...":"Đặt lại mật khẩu"}</button>{error&&<div className="notice">{error}</div>}</form></>}<small className="login-footer">CỔNG THÔNG TIN HỖ TRỢ CNTT VINATEX ĐÀ NẴNG</small></section></main>}
function Loading(){return <main className="login-page"><section className="login-card"><div className="login-brand"><img src="/brand/vinatex-da-nang.png" alt="Vinatex Đà Nẵng"/></div><span className="eyebrow">Bảo mật tài khoản</span><h1>Đặt lại mật khẩu</h1><p className="login-subtitle">Đang kiểm tra liên kết đặt lại mật khẩu...</p></section></main>}
export default function ResetPassword(){return <Suspense fallback={<Loading/>}><ResetPasswordForm/></Suspense>}
