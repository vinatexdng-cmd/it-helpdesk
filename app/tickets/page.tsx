"use client";
import { useEffect,useMemo,useState } from "react";
type Ticket={ticket_no:string;title:string;unit:string;category:string;priority:string;status:string;assignee:string|null};
export default function Tickets(){const [rows,setRows]=useState<Ticket[]>([]);const [q,setQ]=useState("");const [status,setStatus]=useState("All");useEffect(()=>{fetch("/api/tickets").then(r=>r.json()).then(j=>setRows(j.tickets||[])).catch(()=>setRows([]))},[]);const filtered=useMemo(()=>rows.filter(x=>(status==="All"||x.status===status)&&Object.values(x).join(" ").toLowerCase().includes(q.toLowerCase())),[rows,q,status]);return <main className="container"><div className="hero"><span className="eyebrow">IT Operations</span><h1>Ticket / Incident</h1><p>Quản lý yêu cầu hỗ trợ và sự cố IT.</p><a className="button" href="/tickets/new">+ Tạo Ticket</a></div><section className="panel"><div className="toolbar"><input className="search" value={q} onChange={e=>setQ(e.target.value)} placeholder="Tìm Ticket, đơn vị, thiết bị..."/><select value={status} onChange={e=>setStatus(e.target.value)}><option>All</option><option>Open</option><option>Assigned</option><option>In Progress</option><option>Resolved</option><option>Closed</option></select></div><div className="table-wrap"><table><thead><tr><th>Ticket</th><th>Tiêu đề</th><th>Đơn vị</th><th>Loại</th><th>Ưu tiên</th><th>Trạng thái</th><th>Phụ trách</th></tr></thead><tbody>{filtered.map(x=><tr key={x.ticket_no}><td><a href={"/tickets/"+encodeURIComponent(x.ticket_no)}><strong>{x.ticket_no}</strong></a></td><td>{x.title}</td><td>{x.unit||"-"}</td><td>{x.category}</td><td>{x.priority}</td><td>{x.status}</td><td>{x.assignee||"Chưa phân công"}</td></tr>)}{!filtered.length&&<tr><td colSpan={7}>Chưa có ticket hoặc chưa cấu hình DATABASE_URL.</td></tr>}</tbody></table></div></section></main>}