"use client";import{useEffect,useState}from"react";export default function Branding(){const[logo,setLogo]=useState("/brand/vinatex-da-nang.png"),[msg,setMsg]=useState("");useEffect(()=>{fetch("/api/branding").then(r=>r.json()).then(j=>j.logo&&setLogo(j.logo))},[]);function choose(e:React.ChangeEvent<HTMLInputElement>){const f=e.target.files?.[0];if(!f)return;if(!["image/png","image/jpeg","image/webp"].includes(f.type)){setMsg("Vui lòng chọn PNG, JPG hoặc WEBP.");return}if(f.size>1024*1024){setMsg("Ảnh vượt quá 1 MB.");return}const rd=new FileReader();rd.onload=()=>{const img=new Image();img.onload=()=>{const ratio=img.width/img.height;if(ratio<.75||ratio>1.35){setMsg("Logo nên gần tỷ lệ vuông (khuyến nghị 1:1) để hiển thị chuẩn.");return}setLogo(String(rd.result));setMsg(`Đã chọn ảnh ${img.width} × ${img.height}px. Nhấn Lưu logo để áp dụng.`)};img.src=String(rd.result)};rd.readAsDataURL(f)}async function save(){setMsg("Đang lưu...");const r=await fetch("/api/admin/branding",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({logo})});const j=await r.json();setMsg(r.ok?"Đã cập nhật logo toàn hệ thống. Tải lại trang để xem thay đổi.":j.error||"Không thể lưu logo")};return <main className="container branding-page"><div className="hero"><span className="eyebrow">QUẢN TRỊ GIAO DIỆN</span><h1>Logo hệ thống</h1><p>Quản trị viên có thể thay logo mà không cần sửa mã nguồn.</p></div><section className="panel branding-upload"><div className="branding-preview"><img src={logo} alt="Xem trước logo"/></div><div className="branding-help"><h2>Tải logo Vinatex Đà Nẵng</h2><p>Khuyến nghị ảnh PNG hoặc WEBP nền trong suốt/trắng, tỷ lệ <strong>1:1</strong>.</p><p>Kích thước phù hợp: <strong>512 × 512 px</strong> hoặc tối thiểu 256 × 256 px. Dung lượng tối đa 1 MB.</p><input type="file" accept="image/png,image/jpeg,image/webp" onChange={choose}/><div className="branding-note">Hệ thống dùng <strong>object-fit: contain</strong>, vì vậy logo luôn giữ nguyên tỷ lệ và không bị kéo giãn hoặc cắt mất nội dung.</div><div className="branding-actions"><button type="button" onClick={save}>Lưu logo</button></div>{msg&&<div className="notice">{msg}</div>}</div></section></main>}
