import { NextRequest, NextResponse } from "next/server";
import { getSession } from "@/lib/session";
import { getPool } from "@/lib/db";
import { listHelpdeskDocuments, loadMarkdown } from "@/lib/github";
type Doc=Awaited<ReturnType<typeof listHelpdeskDocuments>>[number];
const STOP=new Set(["toi","em","anh","chi","la","bi","va","voi","cua","cho","duoc","khong","mot","nhung","nay","can","ho","tro"]);
function normalize(s:string){return s.toLowerCase().normalize("NFD").replace(/[\u0300-\u036f]/g,"").replace(/đ/g,"d").replace(/[^a-z0-9./\\-]+/g," ").replace(/\s+/g," ").trim()}
function terms(input:string){return [...new Set(normalize(input).split(" ").filter(x=>x.length>=2&&!STOP.has(x)))]}
function rank(d:Doc,words:string[],category:string){const title=normalize(d.title),path=normalize(d.path);let n=0;for(const w of words){if(title.includes(w))n+=10;if(path.includes(w))n+=6;}if(path.includes("troubleshooting-cases"))n+=8;if(category&&path.includes(normalize(category)))n+=10;return n}
function priorityHint(ticket:any){const q=normalize([ticket.title,ticket.description,ticket.category].filter(Boolean).join(" "));if(/ransomware|ma hoa file|toan bo|ca nha may|ca don vi|he thong dung/.test(q))return "Critical";if(/ca phong|nhieu nguoi|mat mang|khong lam viec duoc/.test(q))return "High";return ticket.priority||"Normal"}
function responseText(j:any){for(const o of j?.output||[])if(o.type==="message")for(const c of o.content||[])if(c.type==="output_text"&&c.text)return c.text;return j?.output_text||""}
function webSources(j:any){const out:any[]=[];for(const o of j?.output||[]){if(o.type==="web_search_call")for(const s of o.action?.sources||[])if(s.url)out.push({title:s.title||s.url,url:s.url,path:s.url,type:"web",relevance:0});if(o.type==="message")for(const c of o.content||[])for(const a of c.annotations||[])if(a.type==="url_citation"&&a.url)out.push({title:a.title||a.url,url:a.url,path:a.url,type:"web",relevance:0});}return [...new Map(out.map(x=>[x.url,x])).values()].slice(0,8)}
export async function POST(req:NextRequest){try{const u=await getSession();if(!u)return NextResponse.json({error:"Unauthorized"},{status:401});const {ticketNo}=await req.json();if(!ticketNo)return NextResponse.json({error:"ticketNo is required"},{status:400});const apiKey=process.env.AI_API_KEY;if(!apiKey)return NextResponse.json({error:"OpenAI chưa được cấu hình. Hãy khai báo AI_API_KEY trên Vercel."},{status:503});const p=getPool();const t=await p.query(u.role==="user"?"SELECT * FROM tickets WHERE ticket_no=$1 AND requester_email=$2":"SELECT * FROM tickets WHERE ticket_no=$1",u.role==="user"?[ticketNo,u.email]:[ticketNo]);if(!t.rowCount)return NextResponse.json({error:"Không tìm thấy yêu cầu"},{status:404});const ticket=t.rows[0];const comments=await p.query("SELECT author,body,created_at FROM ticket_comments WHERE ticket_id=$1 ORDER BY created_at ASC",[ticket.id]).catch(()=>({rows:[]}));const audits=await p.query("SELECT actor,action,details,created_at FROM audit_logs WHERE ticket_id=$1 ORDER BY created_at ASC",[ticket.id]).catch(()=>({rows:[]}));const query=[ticket.title,ticket.description,ticket.category,ticket.asset,ticket.unit,...comments.rows.map((x:any)=>x.body)].filter(Boolean).join(" "),words=terms(query);const docs=await listHelpdeskDocuments();const candidates=docs.map(d=>({...d,score:rank(d,words,ticket.category||"")})).filter(d=>d.score>0).sort((a,b)=>b.score-a.score).slice(0,6);const sources:any[]=[];for(const d of candidates){try{const content=await loadMarkdown(d.path);sources.push({...d,content:content.slice(0,6500)})}catch{}}const context=sources.map((d,i)=>`SOURCE ${i+1}\nTitle: ${d.title}\nPath: ${d.path}\nType: ${d.type}\nRelevance: ${d.score}\nContent:\n${d.content}`).join("\n\n---\n\n");const base=(process.env.AI_BASE_URL||"https://api.openai.com/v1").replace(/\/$/,""),model=process.env.AI_MODEL||"gpt-5.6-luna",hint=priorityHint(ticket);const format=`Trả lời tiếng Việt theo cấu trúc: 1. TÓM TẮT SỰ CỐ; 2. ĐÁNH GIÁ MỨC ĐỘ; 3. NGUYÊN NHÂN CÓ THỂ; 4. CHECKLIST CHẨN ĐOÁN; 5. HƯỚNG XỬ LÝ; 6. ĐIỀU KIỆN CHUYỂN CẤP; 7. GỢI Ý PHẢN HỒI NGƯỜI DÙNG; 8. NGUỒN THAM KHẢO. Không khẳng định nguyên nhân hoặc đã sửa xong khi chưa có bằng chứng. Ưu tiên tài liệu chính thức của Microsoft, Cisco, Apple, Google, nhà sản xuất/phần mềm liên quan; nguồn cộng đồng chỉ dùng bổ trợ và phải phân biệt rõ.`;const needWeb=sources.length===0||(sources[0]?.score||0)<60;let answer="",returnedSources:any[]=sources.map(s=>({title:s.title,path:s.path,type:s.type,url:s.url,relevance:s.score})),researchMode="internal";if(needWeb){const input=`Bạn là Trợ lý kỹ thuật IT Helpdesk Vinatex Đà Nẵng. Kho kiến thức nội bộ chưa có nguồn đủ mạnh cho ticket này. Hãy dùng web search để nghiên cứu từ nhiều nguồn đáng tin cậy, đối chiếu trước khi hướng dẫn. ${format}\n\nTICKET:\n${JSON.stringify(ticket)}\n\nBÌNH LUẬN:\n${JSON.stringify(comments.rows)}\n\nTÀI LIỆU NỘI BỘ GẦN NHẤT (có thể chỉ liên quan yếu):\n${context||"Không có"}`;const wr=await fetch(base+"/responses",{method:"POST",headers:{"Content-Type":"application/json","Authorization":"Bearer "+apiKey},body:JSON.stringify({model,input,tools:[{type:"web_search"}],tool_choice:"auto",include:["web_search_call.action.sources"],max_output_tokens:2600})});const wj=await wr.json();if(!wr.ok)return NextResponse.json({error:wj?.error?.message||"OpenAI web research error"},{status:502});answer=responseText(wj)||"OpenAI không trả về nội dung.";const ws=webSources(wj);returnedSources=[...returnedSources,...ws];researchMode="web";}else{const prompt=`Bạn là Trợ lý kỹ thuật IT Helpdesk Vinatex Đà Nẵng. Ưu tiên SOURCE và lịch sử ticket. ${format}\n\nTICKET:\n${JSON.stringify(ticket)}\n\nBÌNH LUẬN:\n${JSON.stringify(comments.rows)}\n\nNHẬT KÝ:\n${JSON.stringify(audits.rows)}\n\nKNOWLEDGE BASE:\n${context}`;const r=await fetch(base+"/chat/completions",{method:"POST",headers:{"Content-Type":"application/json","Authorization":"Bearer "+apiKey},body:JSON.stringify({model,messages:[{role:"system",content:"Bạn là trợ lý kỹ thuật Helpdesk. Chẩn đoán có kiểm chứng, ưu tiên an toàn và khả năng truy vết."},{role:"user",content:prompt}],max_completion_tokens:2200})});const j=await r.json();if(!r.ok)return NextResponse.json({error:j?.error?.message||"OpenAI provider error"},{status:502});answer=j?.choices?.[0]?.message?.content||"OpenAI không trả về nội dung.";}return NextResponse.json({answer,priorityHint:hint,sources:returnedSources,researchMode,canProposeKnowledge:true});}catch(e){return NextResponse.json({error:e instanceof Error?e.message:"OpenAI analysis failed"},{status:500})}}
