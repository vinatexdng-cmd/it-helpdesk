"use client";import {useState} from "react";import {useRouter} from "next/navigation";
export default function Login(){const [email,setEmail]=useState(""),[password,setPassword]=useState(""),[error,setError]=useState(""),router=useRouter();async function submit(e:React.FormEvent){e.preventDefault();setError("");const r=await fetch("/api/auth/login",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({email,password})});const j=await r.json();if(!r.ok){setError(j.error||"Đăng nhập thất bại");return}router.replace("/")};return <main className="container"><div className="hero"><span className="eyebrow">Vinatex IT Operations</span><h1>Đăng nhập Helpdesk</h1><p>Sử dụng tài khoản IT Helpdesk được cấp.</p></div><form className="panel form" style={{maxWidth:520}} onSubmit={submit}><label>Email<input required type="email" value={email} onChange={e=>setEmail(e.target.value)}/></label><label>Mật khẩu<input required type="password" value={password} onChange={e=>setPassword(e.target.value)}/></label><button>Đăng nhập</button>{error&&<div className="notice">{error}</div>}</form></main>}